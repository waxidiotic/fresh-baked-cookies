const validateCommand = () => {
  // validate action is either 'give' or 'send'
  // validate recipient is in <UXXXXXX> format
  // validate quantity is a String(number)
};

const getRecipient = () => {
  // parse user id from <USER_ID|USER_NAME> where the | and USER_NAME may NOT be present
}